import React from "react"
import styling from "../modern-normalize.css"

const ModernNormalize = ({ children }) => (
  <div className="modern_normalize_styling" style={styling}>
    {children}
  </div>
)

export default ModernNormalize
